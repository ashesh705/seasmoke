"""
Run the CLI application.

Import and run the CLI application here so that once installed,
the app can be run as a module. Also, this is the script to run
if running the application without installing.
"""

from seasmoke.app import app

if __name__ == "__main__":
    app()
